{
  "name": "@grass/database",
  "version": "0.1.0",
  "description": "Postgres schema + migrations + RLS policies. Mirrors twin-model contracts from architecture/twin/.",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "test": "bun test src/"
  },
  "dependencies": {
    "@grass/auth": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5.7.2"
  }
}
